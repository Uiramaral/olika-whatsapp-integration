# 📦 GitHub Actions Workflows

Este diretório contém os workflows de CI/CD para deploy automático no Railway.

---

## 📁 Arquivos

- **`deploy.yml`** - Deploy automático em push para `main`/`master`
- **`deploy-manual.yml`** - Deploy manual com opções customizáveis

---

## 🚀 Uso Rápido

### Deploy Automático

Basta fazer push para `main`:

```bash
git push origin main
```

O deploy será acionado automaticamente.

### Deploy Manual

1. Acesse: **GitHub → Actions → Railway Deploy - Manual Trigger**
2. Clique em **Run workflow**
3. Escolha as opções desejadas
4. Execute

---

## ⚙️ Configuração

Veja o guia completo em: `GITHUB_ACTIONS_RAILWAY_DEPLOY.md`

---

**Workflows configurados! ✅**

